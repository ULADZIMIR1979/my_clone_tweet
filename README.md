# Twitter Clone - Сервис микроблогов

Это веб-приложение представляет собой сервис микроблогов, аналогичный Twitter, разработанный с использованием Flask и PostgreSQL. Проект реализует основные функции микроблогинга: создание твитов, подписка на пользователей, лайки и получение ленты твитов.

## Функциональные возможности

- Пользователь может добавить новый твит
- Пользователь может удалить свой твит
- Пользователь может зафоловить другого пользователя
- Пользователь может отписаться от другого пользователя
- Пользователь может отмечать твит как понравившийся
- Пользователь может убрать отметку «Нравится»
- Пользователь может получить ленту из твитов, отсортированных по популярности, от пользователей, на которых он подписан
- Твит может содержать картинку

## Технические характеристики

- **Бэкенд**: Flask
- **База данных**: PostgreSQL
- **Фронтенд**: HTML, CSS, JavaScript (взят из директории dist)
- **Прокси-сервер**: Nginx
- **Контейнеризация**: Docker, Docker Compose
- **Документация API**: Swagger
- **Тестирование**: pytest

## Архитектура

Проект использует архитектуру с тремя основными контейнерами:
1. **db**: Контейнер с PostgreSQL базой данных
2. **web**: Контейнер с Flask приложением
3. **nginx**: Контейнер с Nginx сервером, который выступает в роли обратного прокси

## Установка и запуск

### Требования

- Docker
- Docker Compose

### Запуск приложения

1. Убедитесь, что Docker и Docker Compose установлены и запущены в вашей системе:

Для Linux:
```bash
sudo systemctl start docker
sudo systemctl enable docker
```

2. Склонируйте репозиторий:

```bash
git clone <URL_репозитория>
cd my_clone_tweet
```

3. Запустите приложение с помощью Docker Compose:

```bash
docker-compose up -d
```

4. Приложение будет доступно по адресу: `http://127.0.0.1:5000/`

5. Документация к API будет доступна по адресу: `http://127.0.0.1:5000/api/docs/`

### Использование

1. Для взаимодействия с API используется заголовок `api-key`, который идентифицирует пользователя
2. Примеры API ключей для тестовых пользователей:
   - `user1_api_key` - Иван Иванов
   - `user2_api_key` - Мария Смирнова
   - `user3_api_key` - Алексей Попов
   - `user4_api_key` - Елена Кузнецова
   - `user5_api_key` - Дмитрий Волков
   - `test` - Test User

## Структура проекта

```
my_clone_tweet/
├── 📁 Основные файлы
│   ├── app.py                 # Основное Flask приложение
│   ├── requirements.txt       # Зависимости Python
│   ├── docker-compose.yml     # Docker Compose конфигурация
│   ├── Dockerfile            # Docker-образ для Flask
│   ├── nginx.conf            # Конфигурация Nginx
│   ├── init_db.py            # Инициализация базы данных
│   ├── init.sql              # SQL скрипт для БД
│   ├── add_test_user.py      # Добавление тестовых пользователей
│   └── swagger_spec.json     # Swagger спецификация
│
├── 📁 Модели и маршруты
│   ├── models/               # Модели SQLAlchemy
│   │   ├── __init__.py
│   │   └── models.py        # User, Tweet, Media, Follow, Like
│   │
│   ├── routes/               # Маршруты приложения
│   │   ├── api.py           # API endpoints (твиты, пользователи, медиа)
│   │   ├── static.py        # Статические маршруты (фронтенд)
│   │   └── swagger.py       # Swagger документация
│   │
│   └── utils/               # Вспомогательные утилиты
│       ├── auth.py          # Аутентификация по API ключу
│       └── validators.py    # Валидация данных
│
├── 📁 Фронтенд
│   └── dist/                # Сборка Vue.js фронтенда
│       ├── index.html       # Главная страница
│       ├── css/             # Стили
│       └── js/              # JavaScript файлы
│
├── 📁 Данные и медиа
│   ├── uploads/             # Загруженные медиафайлы (картинки)
│   ├── app.db              # SQLite база данных (для разработки)
│   └── instance/
│       └── app.db          # Instance база данных
│
├── 📁 Тестирование
│   └── tests/              # Набор тестов pytest
│       ├── test_api.py              # Тесты API endpoints
│       ├── test_models.py           # Тесты моделей
│       ├── test_auth_utils.py       # Тесты аутентификации
│       ├── test_validators.py       # Тесты валидаторов
│       ├── test_api_exceptions.py   # Тесты исключений
│       └── conftest.py              # Фикстуры pytest
│
├── 📁 Документация и отчеты
│   ├── htmlcov/            # Отчет о покрытии кода тестами
│   └── README.md           # Документация проекта
│
└── 📁 Окружение
    ├── .venv/              # Виртуальное окружение Python
    └── venv/               # Альтернативное виртуальное окружение
```

## API Endpoints

### Создание твита
```
POST /api/tweets
Headers: api-key: <ключ_пользователя>
Body: {"tweet_data": "Текст твита", "tweet_media_ids": [1, 2]}
```

### Загрузка медиафайла
```
POST /api/medias
Headers: api-key: <ключ_пользователя>
Form: file=<файл_изображения>
```

### Удаление твита
```
DELETE /api/tweets/<id>
Headers: api-key: <ключ_пользователя>
```

### Постановка лайка
```
POST /api/tweets/<id>/likes
Headers: api-key: <ключ_пользователя>
```

### Удаление лайка
```
DELETE /api/tweets/<id>/likes
Headers: api-key: <ключ_пользователя>
```

### Подписка на пользователя
```
POST /api/users/<id>/follow
Headers: api-key: <ключ_пользователя>
```

### Отписка от пользователя
```
DELETE /api/users/<id>/follow
Headers: api-key: <ключ_пользователя>
```

### Получение ленты твитов
```
GET /api/tweets
Headers: api-key: <ключ_пользователя>
```

### Получение информации о текущем пользователе
```
GET /api/users/me
Headers: api-key: <ключ_пользователя>
```

### Получение информации о пользователе
```
GET /api/users/<id>
```

## Тестирование

Для запуска тестов выполните:

```bash
# Внутри контейнера Flask-приложения
pytest tests/ -v

# Или с использованием Docker
docker-compose exec web pytest tests/ -v
```

Покрытие кода тестами составляет 94%, что является отличным результатом для реального проекта.

## Документация

Документация к API доступна через Swagger UI по адресу: `http://127.0.0.1:5000/api/docs/`

## Безопасность

- Все API-запросы требуют заголовок `api-key` для аутентификации
- Пользователи могут удалять только свои собственные твиты
- Проверки на дублирование лайков и подписок
- Валидация входных данных

## Масштабируемость

Проект разработан с учетом возможности масштабирования:
- Использование PostgreSQL для надежного хранения данных
- Контейнеризация для простого развертывания
- Четкая архитектура с разделением ответственности